@model MvcProyectoExcel.ViewModels.StudentListViewModel
@{
    ViewData["Title"] = "Students";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <h1 class="h3 mb-0">Students</h1>
</div>

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <div class="alert alert-danger">@Model.ErrorMessage</div>
}
else
{
    <form method="get" class="row g-3 align-items-end mb-4">
        <div class="col-md-6">
            <label class="form-label" for="courseId">Course</label>
            <select class="form-select" id="courseId" name="courseId" onchange="this.form.submit()">
                @foreach (var course in Model.Courses)
                {
                    if (Model.SelectedCourseId == course.Id)
                    {
                        <option value="@course.Id" selected="selected">@course.Name (@course.StudentCount students)</option>
                    }
                    else
                    {
                        <option value="@course.Id">@course.Name (@course.StudentCount students)</option>
                    }
                }
            </select>
        </div>
        <div class="col-md-2">
            <button type="submit" class="btn btn-outline-primary">Load</button>
        </div>
    </form>

    @if (!string.IsNullOrEmpty(Model.SelectedCourseName))
    {
        <p class="text-muted">Course: <strong>@Model.SelectedCourseName</strong></p>
    }

    <div class="table-responsive">
        <table class="table table-striped table-hover align-middle">
            <thead class="table-light">
                <tr>
                    <th>#</th>
                    <th>Name</th>
                    <th>Email</th>
                    <th>Status</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.Students.Count == 0)
                {
                    <tr>
                        <td colspan="4" class="text-center text-muted">No students enrolled in this course.</td>
                    </tr>
                }
                else
                {
                    var index = 1;
                    foreach (var student in Model.Students)
                    {
                        <tr>
                            <td>@index</td>
                            <td>@student.FullName</td>
                            <td>@student.Email</td>
                            <td>
                                @if (student.IsActive)
                                {
                                    <span class="badge text-bg-success">Active</span>
                                }
                                else
                                {
                                    <span class="badge text-bg-secondary">Inactive</span>
                                }
                            </td>
                        </tr>
                        index++;
                    }
                }
            </tbody>
        </table>
    </div>
}
